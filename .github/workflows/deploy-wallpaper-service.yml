name: Build Wallpaper Nexus

on:
  push:
    branches: ['master']
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Publish
      shell: pwsh
      run: |
        $tag = '${{ github.ref_name }}'
        $version = if ($tag -match '^v') { $tag.TrimStart('v') } else { '1.0.0' }
        dotnet publish Excogitated.WallpaperNexus/Excogitated.WallpaperNexus.csproj `
          --configuration Release `
          --runtime win-x64 `
          --self-contained true `
          -p:PublishSingleFile=true `
          -p:PublishTrimmed=false `
          -p:Version=$version `
          --output ./publish/WallpaperNexus

    - name: Create GitHub Release
      shell: pwsh
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        $isTag = '${{ github.ref }}' -match '^refs/tags/'
        $releaseTag = if ($isTag) { '${{ github.ref_name }}' } else { 'latest' }
        $releaseTitle = if ($isTag) { "Wallpaper Nexus ${{ github.ref_name }}" } else { 'Wallpaper Nexus (latest)' }
        $notes = if ($isTag) { '--generate-notes' } else { '--notes "Automated build from master."' }
        gh release delete $releaseTag --yes 2>&1 | Out-Null
        Invoke-Expression "gh release create '$releaseTag' --title '$releaseTitle' $notes ./publish/WallpaperNexus/WallpaperNexus.exe"
